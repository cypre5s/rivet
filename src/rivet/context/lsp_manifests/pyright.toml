server_id = "pyright"
language_ids = ["python"]
suffixes = [".py", ".pyi"]
executable_candidates = ["pyright-langserver", "basedpyright-langserver"]
arguments = ["--stdio"]
idle_timeout_seconds = 300
request_timeout_seconds = 10
max_restarts = 1

[initialization_options]
